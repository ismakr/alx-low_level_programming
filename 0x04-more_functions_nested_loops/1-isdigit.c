#include "main.h"
/**
 * _isdigit - prints str using only _putchar
 * @c: Description of parameter x
(* a blank line
 * Description: Longer description of the function)?
(* section header: Section description)*
 * Return: (1) if its dig (0) oth
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 56)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
